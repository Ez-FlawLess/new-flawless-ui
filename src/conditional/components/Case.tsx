import { ReactElement, PropsWithChildren } from "react";
import { CasePropsI } from "../types/Case.types";

export const Case = <T,>(props: PropsWithChildren<CasePropsI<T>>): ReactElement | null => {
    return null
}